import React from 'react'
import "./styles/rules.css"

function Rules(props) {
  return (
        <div className='rules-button upper' onClick={props.setRules}>
            rules
        </div>
  )
}

export default Rules